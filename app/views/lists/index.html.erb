<div class="container">
  <h1>My lists</h1>
  <ul>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list) %></li>
    <% end %>
  </ul>
  <%= link_to 'Add a List', lists_new_path(@list) %>
  <p></p>
</div>
